import React from 'react';
import { Text, View } from 'react-native';

export default function EmployeeForm({ onBack }: { onBack: () => void })
{
    {
        return(
            <View>
                <Text>EmployeeForm</Text>
            </View>
        )
    }
}